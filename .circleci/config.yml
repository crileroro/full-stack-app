version: 2.1

commands:
  test:
    parameters:
      myparameter:
        type: string
        default: "crileroro"
    steps:
      - run: echo "Hello <<parameters.myparameter>>"

jobs:
  my-job:
    docker:
      - image: cimg/base:stable
    steps:
      - test: 
          myparameter: "test-12345"
          
